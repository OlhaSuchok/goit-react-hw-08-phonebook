import styled from '@emotion/styled';

export const ErrorMessageText = styled.p`
  text-align: center;
  margin-top: 0;
  margin-bottom: 0;
  font-family: emoji;
  color: #696969;
  font-size: 28px;
  font-weight: 600;
`;
